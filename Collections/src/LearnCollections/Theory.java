package LearnCollections;

public class Theory {

	public static void main(String[] args) {
		// collections are in interfaces and classes
		//interfaces-- highest level of interfaces in collections is iterable>>sub interface-collection
//and in collection extends/sub interface the queue,list,set,map
		//and classes extends the priority queue,array list,linked list, hash set,linked hash set, hash map,hash table
	//collections  stores object within it.
		//can't work on the primitive data type so it provides an object
		//wrapper class means they do a wrapping when we have primitive data types and non wrapping of an objects when we want to use.
		//widely used hash map , hash set, array list
		
		System.out.println("collectons theory");
	
	
	}

}
